import styles from "./style.module.scss";
import {ButtonProps} from "@/types/ui/button/button.type";

const Button = ({size, color, onClick}: ButtonProps) => {
    return (
        <button>

        </button>
    )
}

export default Button;